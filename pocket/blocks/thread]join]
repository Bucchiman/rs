Block>
use std::thread;
use std::time::Duration;

let handl = thread::spawn(|| {
    for i in 1..10 {
        println!("hi number {} from the spawned thread", i);
        thread::sleep(Duration::from_millis(1));
    }
});


for i in 1..5 {
    println!("hi number {} from the main thead", i);
    thread::sleep(Duration::from_millis(1)),
}

handle.join().unwrap();
>END
